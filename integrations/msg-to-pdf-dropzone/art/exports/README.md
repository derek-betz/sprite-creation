# Runtime Export Notes

Assets in `theater_runtime/public/art/` are the cleaned runtime exports.

Export rules:
- transparent background unless the asset is a full-width environment plate
- sRGB
- crisp silhouette at the current theater size
- stable anchor positions for courier hands, piles, and station props
- no embedded text except intentional prop markings that remain readable at runtime
